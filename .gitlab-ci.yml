stages:
  - build

build-job:
  stage: build
  script:
    - wails build -clean    # 使用 Wails 构建应用
  artifacts:
    paths:
      - build/bin/          # 上传构建后的文件（路径根据 Wails 实际输出调整）
    expire_in: 1 week       # 产物保留时间
